import React from 'react';
import { Typography } from '@mui/material';

const Dashboard = () => (
  <Typography variant="h4">Dashboard - Ticket Trends & KPIs</Typography>
);

export default Dashboard;